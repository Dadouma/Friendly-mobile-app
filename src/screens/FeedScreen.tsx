import { StyleSheet, Text, View } from "react-native";

const FeedScreen = () => {
  return (
    <View style={styles.container}>
      <Text>salut salit</Text>
    </View>
  );
};
const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
  },
});
export default FeedScreen;
